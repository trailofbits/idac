interface:
  display_name: "idac"
  short_description: "Use idac for IDA CLI reversing, mutation, and class recovery"
  default_prompt: "Use $idac for reverse-engineering work through the ida-nexus-backed local CLI against a live IDA GUI target, an existing .i64 database, or a binary that IDA can open, including decompilation, xrefs, type and class recovery, safe mutations, target selection, troubleshooting, and IDAPython when needed."

policy:
  allow_implicit_invocation: true
